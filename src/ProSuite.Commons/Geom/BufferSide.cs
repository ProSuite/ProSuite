namespace ProSuite.Commons.Geom
{
	/// <summary>
	/// The side(s) of a line to buffer, seen in the digitizing direction.
	/// </summary>
	public enum BufferSide
	{
		Left = 0,
		Right = 1,
		Both = 2
	}
}
