<controls:ProWindow x:Class="ProSuite.Commons.AGP.Help.AboutWindow"
        x:Name="AboutWindowName"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProSuite.Commons.AGP.Help"
        xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
        d:DataContext="{d:DesignInstance {x:Type local:AboutViewModel}, IsDesignTimeCreatable=False}"
        mc:Ignorable="d"
        Title="About"
        Height="480" MinHeight="256"
        Width="640" MinWidth="400"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
	<DockPanel Margin="12">
		<DockPanel DockPanel.Dock="Bottom" LastChildFill="False" Margin="0,12,0,0">
			<Button DockPanel.Dock="Right" Margin="8,0,0,0" MinWidth="60"
			        Content="Close" IsDefault="True" IsCancel="True" TabIndex="2" ClickMode="Release"
			        Command="{Binding CloseCommand}"
			        CommandParameter="{Binding ElementName=AboutWindowName}" />
			<Button DockPanel.Dock="Right" Margin="8,0,0,0" MinWidth="60"
			        Content="Copy" TabIndex="1" ClickMode="Release"
			        ToolTip="Copy to clipboard as plain text"
			        Command="{Binding CopyCommand}"
			        CommandParameter="{Binding ElementName=AboutWindowName}" />
		</DockPanel>
		<TextBlock DockPanel.Dock="Top" Margin="0,0,0,12" FontWeight="Bold" FontSize="14">
			<Run Text="{Binding Heading}" />
		</TextBlock>
		<DataGrid ItemsSource="{Binding AboutItems}" AutoGenerateColumns="False"
                  IsReadOnly="True" Style="{DynamicResource Esri_DataGrid}"
                  x:Name="AboutItemDataGrid">
			<DataGrid.GroupStyle>
				<GroupStyle>
					<GroupStyle.HeaderTemplate>
						<DataTemplate>
							<TextBlock Padding="14,2,4,2" FontWeight="Bold">
								<Run Text="{Binding Path=Name,Mode=OneWay}" />
							</TextBlock>
						</DataTemplate>
					</GroupStyle.HeaderTemplate>
				</GroupStyle>
			</DataGrid.GroupStyle>
			<DataGrid.Columns>
				<DataGridTextColumn Header="Key" Binding="{Binding Key}" Width="Auto" MinWidth="150" />
				<DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="3*" MinWidth="150">
					<DataGridTextColumn.ElementStyle>
						<Style TargetType="TextBlock">
							<Setter Property="TextWrapping" Value="WrapWithOverflow"/>
							<Setter Property="VerticalAlignment" Value="Top"/>
						</Style>
					</DataGridTextColumn.ElementStyle>
				</DataGridTextColumn>
				<DataGridTextColumn Header="Remark" Binding="{Binding Remark}" Width="*" MinWidth="64">
					<DataGridTextColumn.ElementStyle>
						<Style TargetType="TextBlock">
							<Setter Property="TextWrapping" Value="WrapWithOverflow"/>
							<Setter Property="VerticalAlignment" Value="Top"/>
						</Style>
					</DataGridTextColumn.ElementStyle>
				</DataGridTextColumn>
			</DataGrid.Columns>
		</DataGrid>
	</DockPanel>
</controls:ProWindow>
