<UserControl x:Class="ProSuite.Commons.AGP.LoggerUI.WinFormsLogDockPane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
             xmlns:logging="clr-namespace:ProSuite.Commons.UI.Logging;assembly=ProSuite.Commons.UI"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
	<UserControl.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<extensions:DesignOnlyResourceDictionary
					Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml" />
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</UserControl.Resources>
	<!-- The client-area background follows Pro's theme (light/dark) automatically. The hosted
	     WinForms LogWindowControl is re-colored separately via ApplyDarkTheme() from the view model. -->
	<Grid Background="{DynamicResource Esri_DockPaneClientAreaBackgroundBrush}">
		<WindowsFormsHost>
			<logging:LogWindowControl x:Name="_logWindowControl" />
		</WindowsFormsHost>
	</Grid>
</UserControl>
