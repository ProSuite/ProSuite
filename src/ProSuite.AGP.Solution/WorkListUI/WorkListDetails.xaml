<UserControl x:Class="ProSuite.AGP.Solution.WorkListUI.WorkListDetails"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
             xmlns:local="clr-namespace:ProSuite.AGP.Solution.WorkListUI"
             mc:Ignorable="d"
             d:DesignWidth="400">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary
                    Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <local:StatusEnumBooleanConverter x:Key="StatusEnumBooleanConverter" />
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Content="Quality Condition: " Grid.Row="0" Grid.Column="0" />
        <TextBox Text="{Binding QualityCondition }" Grid.Row="0" Grid.Column="1" />
        <Label Content="Error Description: " Grid.Row="1" Grid.Column="0" />
        <TextBox Text="{Binding ErrorDescription}" Grid.Row="1" Grid.Column="1" />

        <ToolBarTray Grid.Row="2" Grid.Column="0">
            <ToolBar />

        </ToolBarTray>
        <DataGrid Grid.ColumnSpan="2" Grid.Column="0" Grid.Row="3" ItemsSource="{Binding InvolvedObjectRows}">
            <!--<DataGridTextColumn Header="Table" Binding="{Binding Table}" />
            <DataGridTextColumn Header="Subtype" Binding="{Binding KeyField}" />
            <DataGridTextColumn Header="Object ID" Binding="{Binding Oid}" />-->
        </DataGrid>
    </Grid>
</UserControl>
