<UserControl x:Class="ProSuite.AGP.Editing.Erase.DockPaneErase"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
             xmlns:erase="clr-namespace:ProSuite.AGP.Editing.Erase"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=erase:DockPaneEraseViewModelBase}">
	<UserControl.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<extensions:DesignOnlyResourceDictionary
					Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml" />

				<ResourceDictionary
					Source="/ProSuite.Commons.UI;component\ManagedOptions\SettingControls.xaml" />
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</UserControl.Resources>

	<StackPanel HorizontalAlignment="Left" VerticalAlignment="Top">

		<!-- Geometry Type Options -->
		<Label FontWeight="Medium" Content="Geometry Type Options" Margin="0,0,0,0" />
		<CheckBox Template="{StaticResource CentralizableSettingCheckbox}"
		          Content="Allow erasing of polyline features"
		          DataContext="{Binding AllowPolylineErasing}" />

		<CheckBox Template="{StaticResource CentralizableSettingCheckbox}"
		          Content="Allow erasing of multipoint features"
		          DataContext="{Binding AllowMultipointErasing}" />

		<!-- Result geometry options -->
		<Label FontWeight="Medium" Content="Result geometry options" Margin="0,15,0,0" />
		<CheckBox Template="{StaticResource CentralizableSettingCheckbox}"
		          Content="Explode multi-part result geometry into new features"
		          DataContext="{Binding PreventMultipartResults}" />

		<!-- Restore Defaults Button -->
		<Button Template="{StaticResource RestoreDefaultsButton}" />
	</StackPanel>
</UserControl>
